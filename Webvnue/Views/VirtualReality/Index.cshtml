@{ 
    Layout = null;
}


<script src="https://aframe.io/releases/0.5.0/aframe.min.js"></script>
<script src="https://unpkg.com/aframe-text-component&#64^0.4.1/dist/aframe-text-component.min.js"></script> 

<a-scene>
    <a-assets>
        <a-asset-item id="tree" src="~/vrnodels/elexis.dae"></a-asset-item>
    </a-assets>

    

    <a-plane color="black" position="0 0 -4" rotation="-90 0 0" width="4" height="4"></a-plane>

    <a-sky src="#boxTexture" color="#ECECEC"></a-sky>

    <a-text value="WEB" color="#BBB" position="-5 3 -10" scale="10 10 10"></a-text>

    <a-text value="VNUE" color="cornflowerblue" position="-.5 3 -10" scale="10 10 10"></a-text>

    <a-entity collada-model="#tree" position="-1 0.5 -3" rotation="0 45 0">

        <a-animation attribute="position" to="0 2.2 -5" direction="alternate" dur="2000"
                     repeat="indefinite"></a-animation>
        <!-- These animations will start when the box is looked at. -->
        <a-animation attribute="scale" begin="mouseenter" dur="300" to="2.3 2.3 2.3"></a-animation>
        <a-animation attribute="scale" begin="mouseleave" dur="300" to="2 2 2"></a-animation>
        <a-animation attribute="rotation" begin="click" dur="2000" to="360 405 45"></a-animation>
    </a-entity>        
        

    <a-camera>
        <a-cursor></a-cursor>
    </a-camera>

</a-scene>